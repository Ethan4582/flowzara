export default function HomePage() {
  return (
    <div style={{ height: '100vh', display: 'grid', placeItems: 'center' }}>
      <h1 style={{ color: 'var(--muted)' }}>Home — placeholder</h1>
    </div>
  );
}