export default function FeaturesPage() {
  return (
    <div style={{ height: '100vh', display: 'grid', placeItems: 'center' }}>
      <h1 style={{ color: 'var(--muted)' }}>Features — placeholder</h1>
    </div>
  );
}